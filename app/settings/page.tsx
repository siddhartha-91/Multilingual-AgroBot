import SettingsPanel from "@/components/settings/settings-panel";

export default function SettingsPage() {
  return (
    <main className="min-h-screen bg-green-50 p-4 flex justify-center">
      <SettingsPanel />
    </main>
  );
}
